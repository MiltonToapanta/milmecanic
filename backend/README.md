# MilMecanic Backend

API NestJS para MilMecanic ERP.

```bash
cp .env.example .env
npm install
npm run prisma:migrate
npm run prisma:seed
npm run start:dev
```

Swagger: `http://localhost:3000/api/docs`

Credenciales de desarrollo: `admin@milmecanic.local` / `Admin123*`.
